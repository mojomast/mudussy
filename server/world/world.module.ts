import { Module } from '@nestjs/common';

@Module({
  providers: [],
  exports: [],
})
export class WorldModule {
  // World module for managing game world state
  // This would integrate with the engine's world management system
}